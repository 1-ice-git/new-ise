@model NewISE.Models.DBModel.MaggiorazioniFamiliariModel

@{

    //bool callConiuge = Convert.ToBoolean(ViewData["callConiuge"]);

    //decimal idMafFam = Model.idMaggiorazioniFamiliari;
    decimal idTrasferimento = ViewBag.idTrasferimento;

    //decimal idMafFam = ViewBag.idMagFam;

}


<div class="row">
    <div class="col-xs-12" style="padding-left: 10px; padding-right:25px; margin: 10px; border:0">
        <div class="nav nav-justified nav-tabs">
            <ul class="nav nav-tabs tabs-destro" id="tabsAttivitaSospensioni">
                <li id="mnSospensioni">
                    <a data-toggle="tab" href="#tabElencoSospensioni" id="lkElencoSospensioni" onclick="ElencoSospensioni();">Sospensioni</a>
                </li>
                <li id="mnSospensioni">
                    <a data-toggle="tab" href="#tabFormulari_Var" id="lkFormulariVar" onclick="ElencoFormulariInseriti();">Formulari</a>
                </li>
            </ul>
        </div>
        <div class="tab-content" id="tabContenuti_Var" style="border:1px;border-color:red">
            <div class="active tab-pane" id="tabElencoSospensioni"></div>
            <div class="tab-pane" id="tabFormulari_Var"></div>
        </div>
    </div>
</div>





<script type="text/javascript">
    $('#tabsAttivitaSospensioni').tab();

    ElencoSospensioni();



    function ElencoSospensioni() {
        //debugger;
        var rotta = "/Sospensione/ElencoSospensioni";
        var idTrasferimento = parseInt('@idTrasferimento');

        $.ajax({
            type: "POST",
            url: rotta,
            data: { idTrasferimento: idTrasferimento },
            dataType: 'html',
            beforeSend: function () {
                //debugger;
                //VerificaAutenticazione();
                //$("#DialogNewDoc").dialog("destroy");
                //$("#divEffettoLoadAutNoDoc").show("slow");
            },
            success: function (result) {
                debugger;
                //$("#divEffettoLoadAutNoDoc").hide("slow");
                $("#tabElencoSospensioni").empty();
                $("#tabElencoSospensioni").html(result);

                //RicercaDocumenti();

            },
            complete: function () {
                // TEST
                //PulsantiNotificaAttivaMagFam();
            },
            error: function (jqXHR, textStatus, errorThrow) {
                //debugger;
                var msg = errorThrow.err;
                AlertDialog(msg);
            }

        });

    }




</script>


