@model IEnumerable<NewISE.Models.DBModel.MaggiorazioneFigliModel>
@using NewISE.Areas.Parametri.Models.dtObj;

@{
    bool escludiAnnullati = (bool)ViewBag.escludiAnnullati;

}

<p>
    <a id="btnInsNuovaMaggFigli" href="#" onclick="NuovaMaggiorazioneFigli();">Inserisci Maggiorazione Figli</a>
</p>

<p>
    <div class="form-group">
        @Html.Label("lblTipologiaFiglio0", "Seleziona Tipologia Figlio", new { @class = "control-label" })
        <div id="divCombo">
            <div class="select2-container select2-container--classic">
                @Html.DropDownList("idTipologiaFiglio", ViewBag.LivelliList as IEnumerable<SelectListItem>, new { @class = "form-control select2 select2-select", @onchange = "PrelevaPVMaggFiglio();", @onClick = "PrelevaPVMaggFiglio();" })
            </div>
        </div>
    </div>
</p>
<table id="tbIndBase" class="display" cellspacing="0">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.dataInizioValidita)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.dataFineValidita)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.percentualeFigli)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.annullato)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>

                <td style="text-align:center;">
                    @Html.DisplayFor(modelItem => item.dataInizioValidita)
                </td>
                <td style="text-align:center;">
                    @Html.DisplayFor(modelItem => item.dataFineValidita)
                </td>
                <td style="text-align:right;">
                    @Html.DisplayFor(modelItem => item.percentualeFigli)
                </td>
                <td style="text-align:center;">
                    @Html.DisplayFor(modelItem => item.annullato)
                </td>
                <td style="text-align:center;">
                    @using (dtMaggFigli dtib = new dtMaggFigli())
                    {
                        if (dtib.EsistonoMovimentiPrima(item))
                        {
                            <a href="#" onclick="EliminaMaggiorazioneFiglio(@item.idMaggiorazioneFigli );"><img alt="Elimina" src="~/Immagini/Eliminazione.png" width="25" /></a>
                        }
                        else
                        {
                            <a href="#" class="disabled"><img alt="Elimina" src="~/Immagini/EliminazioneDisable.png" width="25" /></a>
                        }
                    }


                </td>
            </tr>
        }
    </tbody>
</table>
<div id="dialog-confirm" title="Elimina maggiorazione figlio?" class="hide">
    <p><span class="ui-icon ui-icon-alert" style="float:left; margin:12px 12px 20px 0;"></span>Si vuole procedere con l'eliminazione della maggiorazione figlio?</p>
</div>

<script type="text/javascript">
    $('.display').DataTable({
        "language": {
            "sEmptyTable": "Nessun dato presente nella tabella",
            "sInfo": "Vista da _START_ a _END_ di _TOTAL_ elementi",
            "sInfoEmpty": "Vista da 0 a 0 di 0 elementi",
            "sInfoFiltered": "(filtrati da _MAX_ elementi totali)",
            "sInfoPostFix": "",
            "sInfoThousands": ".",
            "sLengthMenu": "Visualizza _MENU_ elementi",
            "sLoadingRecords": "Caricamento...",
            "sProcessing": "Elaborazione...",
            "sSearch": "Cerca:",
            "sZeroRecords": "La ricerca non ha portato alcun risultato.",
            "oPaginate": {
                "sFirst": "Inizio",
                "sPrevious": "Precedente",
                "sNext": "Successivo",
                "sLast": "Fine"
            },
            "oAria": {
                "sSortAscending": ": attiva per ordinare la colonna in ordine crescente",
                "sSortDescending": ": attiva per ordinare la colonna in ordine decrescente"
            }
        }
    });

    $("#idTipologiaFiglio").select2({
        placeholder: "Seleziona la Tipologia Figlio ",
        allowClear: true,
        language: "it",
        width: '150'

    });

    $("#btnInsNuovaMaggFigli").button();

    function PrelevaPVMaggFiglio() {
        //debugger;
        rotta = "/Parametri/ParamMaggFigli/MaggiorazioneFiglioLivello";

        var idTipologiaFiglio = parseInt($("#idTipologiaFiglio").select2('val'));
        var chk = true;

        if (isNaN(idTipologiaFiglio)) {
            $("#divPViewParametri").empty();
            return false;
        }

        if ('@escludiAnnullati' == 'true') {
            chk = false;
        }

        $.ajax({
            type: "POST",
            url: rotta,
            data: { idTipologiaFiglio: idTipologiaFiglio, escludiAnnullati: chk },
            dataType: 'html',
            beforeSend: function () {
                //debugger;
                //VerificaAutenticazione();
                //$("#DialogNewDoc").dialog("destroy");
                //$("#divEffettoLoadAutNoDoc").show("slow");
            },
            success: function (result) {
                //debugger;
                //$("#divEffettoLoadAutNoDoc").hide("slow");
                $("#divPViewParametri").empty();
                $("#divPViewParametri").html(result);

                //RicercaDocumenti();

            },
            complete: function () {
                //$("#divEffettoLoadAutNoDoc").hide("slow");
                //                $("#Centro").getNiceScroll().resize();
            },
            error: function (jqXHR, textStatus, errorThrow) {
                //debugger;
                var msg = errorThrow.err;
                AlertDialog(msg);
            }

        });

    }

    function NuovaMaggiorazioneFiglio() {
        //debugger;

        var rotta = "/Parametri/ParamMaggFigli/NuovaMaggiorazioneFiglio";
        var idTipologiaFiglio = parseInt($("#idTipologiaFiglio").select2('val'));
        var chk = false;

        if (isNaN(idTipologiaFiglio)) {
            //$("#divPViewParametri").empty();
            AlertDialog("Selezionare la tipologia figlio per procedere.");
            return false;
        }

        if ('@escludiAnnullati' == 'true') {
            chk = false;
        }

        $.ajax({
            type: "POST",
            url: rotta,
            data: { idTipologiaFiglio: idTipologiaFiglio, escludiAnnullati: chk },
            dataType: 'html',
            beforeSend: function () {
                //debugger;
                //VerificaAutenticazione();
                //$("#DialogNewDoc").dialog("destroy");
                //$("#divEffettoLoadAutNoDoc").show("slow");
            },
            success: function (result) {
                //debugger;
                //$("#divEffettoLoadAutNoDoc").hide("slow");
                $("#divPViewParametri").empty();
                $("#divPViewParametri").html(result);

                //RicercaDocumenti();

            },
            complete: function () {
                //$("#divEffettoLoadAutNoDoc").hide("slow");
                //                $("#Centro").getNiceScroll().resize();
            },
            error: function (jqXHR, textStatus, errorThrow) {
                //debugger;
                var msg = errorThrow.err;
                AlertDialog(msg);
            }

        });

    }

    function EliminaMaggiorazioneFiglio(id) {
        //debugger;
        var rotta = "/Parametri/ParamMaggFigli/EliminaMaggiorazioneFiglio";

        var idTipologiaFiglio = parseInt($("#idTipologiaFiglio").select2('val'));
        var chk = true;

        if (isNaN(idTipologiaFiglio)) {
            $("#divPViewParametri").empty();
            return false;
        }

        if ('@escludiAnnullati' == 'true') {
            chk = false;
        }

        $('#dialog-confirm').dialog({
            autoOpen: true,
            width: 400,
            buttons: [
                {
                    text: "OK",
                    click: function () {
                        $(this).dialog("close");
                        $.ajax({
                            type: "POST",
                            url: rotta,
                            data: { escludiAnnullati: chk, idTipologiaFiglio: idTipologiaFiglio, idMaggFiglio: id },
                            dataType: 'html',
                            beforeSend: function () {
                                //debugger;
                                //VerificaAutenticazione();
                                //$("#DialogNewDoc").dialog("destroy");
                                //$("#divEffettoLoadAutNoDoc").show("slow");

                            },
                            success: function (result) {
                                //debugger;
                                //$("#divEffettoLoadAutNoDoc").hide("slow");
                                $("#divPViewParametri").empty();
                                $("#divPViewParametri").html(result);

                                //RicercaDocumenti();

                            },
                            complete: function () {
                                //$("#divEffettoLoadAutNoDoc").hide("slow");
                                //                $("#Centro").getNiceScroll().resize();
                            },
                            error: function (jqXHR, textStatus, errorThrow) {
                                //debugger;
                                var msg = errorThrow.err;
                                AlertDialog(msg);
                            }

                        });
                    },
                    "class": "ui-button-danger"
                },
                {
                    text: "Annulla",
                    click: function () {
                        $(this).dialog("close");
                    }
                }
            ]
        });

    }
</script>